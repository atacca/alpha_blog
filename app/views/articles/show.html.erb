<h2 class="inner-pages-heading"><%= @article.title %></h2>

<div class="inner-articles-container">
    <div class="row justify-content-md-center">
      <div class="col-8">
        <div class="card-articles shadow mb-5 bg-white rounded">
          <div class="card-header font-italic">
            by <%= @article.user.username %>
          </div>
          <div class="card-body">
            <div class="card-text text-left"><%= simple_format(@article.description) %></div>
            <%= link_to "Go back", articles_path, class: "btn btn-outline-dark" %>
            <%= link_to "Edit", edit_article_path(@article), class: "btn btn-outline-dark" %>
            <%= link_to "Delete", article_path(@article), class: "btn btn-outline-danger", method: :delete, data: {confirm: "Are you sure?"} %>
          </div>
          <div class="card-footer text-muted">
            <small>Created <%= time_ago_in_words(@article.created_at) %> ago, edited <%= time_ago_in_words(@article.updated_at) %> ago</small>
          </div>
        </div>
      </div>
    </div>
</div>
